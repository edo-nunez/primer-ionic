import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'routes_lifecycle',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
